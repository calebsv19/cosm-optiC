# Animations › deep_render

Placeholder directory for high-quality frame dumps. Update `animSettings.frameDir` to `Animations/deep_render` in `Configs/animation_config.json` (or through the SDL menu) to redirect deep render output here.
