# include › camera

Public interface for the viewport subsystem (`camera.h`). Provides the `Camera` struct, world/screen conversion helpers, zoom/pan controls, and utilities to clamp margins or build preview cameras for the editors.
