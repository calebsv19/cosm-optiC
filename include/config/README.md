# include › config

Exposes configuration structures and persistence helpers.

- `config_manager.h` – Declares `AnimationConfig`, `SceneConfig`, runtime config policies, and load/save entrypoints.
- `config_file_io.h` – Shared config file I/O and directory helpers for fallback loads and JSON parsing.
