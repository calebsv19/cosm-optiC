#ifndef APP_RAY_TRACING_BUILD_IDENTITY_H
#define APP_RAY_TRACING_BUILD_IDENTITY_H

#include <stddef.h>

void ray_tracing_build_identity_format_window_title(char* output,
                                                    size_t output_size,
                                                    const char* base_title);

#endif
