# include › editor

Editor-facing interfaces shared across the scene editing modules.

- `bezier_editor.h` – Bézier point/handle manipulation API.
- `object_editor.h` – Object placement and selection controls.
- `scene_editor.h` – Scene editor container, mode switching, and UI helpers.
- `camera_editor.h` – Stub definitions for future camera controls.
