diff --git a/README.md b/README.md
index fb7af97..edabb65 100644
--- a/README.md
+++ b/README.md
@@ -244,0 +245,16 @@ When you need the simplified Shape format (paths + cubic segments) for other pro
+
+## Typed surface graph producer (M6 source candidate)
+
+Agent scene plane, rect_prism and mesh_asset_instance objects accept a
+`surface_graph` object directly or in `ray_tracing`. Required declaration:
+version 1, required_capability `optic.surface_graph_v1`, color_space `linear`.
+The producer preserves the complete graph, IDs and optional metadata into both
+scene_authoring and scene_runtime outputs. Do not combine it with a material
+prompt or texture stack. The producer validates its envelope; optiC's
+`--validate-surface-graph` and ordinary scene preflight validate node execution,
+connections and source/geometry compatibility. Unknown required capabilities
+fail; this is not Sculpts preview execution of arbitrary shaders. See optiC's
+`docs/surface_material_m6_contract.md` for the node contract and source limits.
+The M6 deterministic producer fixture is generated by optiC's
+`tests/integration/test_surface_graph_m6.py --sculpts-tool <binary>`.
+No desktop package or release is claimed by this source adapter.
diff --git a/docs/agent_scene_authoring_cli.md b/docs/agent_scene_authoring_cli.md
index 6a4ca91..0ca006f 100644
--- a/docs/agent_scene_authoring_cli.md
+++ b/docs/agent_scene_authoring_cli.md
@@ -346,0 +347,16 @@ with screens, baffles, and a taller emitter column.
+
+## Typed surface graph producer (M6 source candidate)
+
+Agent scene plane, rect_prism and mesh_asset_instance objects accept a
+`surface_graph` object directly or in `ray_tracing`. Required declaration:
+version 1, required_capability `optic.surface_graph_v1`, color_space `linear`.
+The producer preserves the complete graph, IDs and optional metadata into both
+scene_authoring and scene_runtime outputs. Do not combine it with a material
+prompt or texture stack. The producer validates its envelope; optiC's
+`--validate-surface-graph` and ordinary scene preflight validate node execution,
+connections and source/geometry compatibility. Unknown required capabilities
+fail; this is not Sculpts preview execution of arbitrary shaders. See optiC's
+`docs/surface_material_m6_contract.md` for the node contract and source limits.
+The M6 deterministic producer fixture is generated by optiC's
+`tests/integration/test_surface_graph_m6.py --sculpts-tool <binary>`.
+No desktop package or release is claimed by this source adapter.
diff --git a/src/Tools/agent_scene_material_flow.c b/src/Tools/agent_scene_material_flow.c
index 898fb16..d07ad03 100644
--- a/src/Tools/agent_scene_material_flow.c
+++ b/src/Tools/agent_scene_material_flow.c
@@ -282,0 +283,2 @@ static bool apply_material_for_object(cJSON* object_materials, const cJSON* item
+    const cJSON* graph = cJSON_GetObjectItemCaseSensitive(item, "surface_graph");
+    if (!graph) graph = cJSON_GetObjectItemCaseSensitive(cJSON_GetObjectItemCaseSensitive(item, "ray_tracing"), "surface_graph");
@@ -285,0 +288,18 @@ static bool apply_material_for_object(cJSON* object_materials, const cJSON* item
+    /* Preserve a declared typed graph as source truth; optiC preflight owns execution validation. */
+    if (graph) {
+        const cJSON* version = cJSON_GetObjectItemCaseSensitive(graph, "version");
+        const char* capability = json_string_or(graph, "required_capability", "");
+        if (!object_id || !object_id[0] || !cJSON_IsObject(graph) || !cJSON_IsNumber(version) ||
+            version->valuedouble != 1 || strcmp(capability, "optic.surface_graph_v1") ||
+            strcmp(json_string_or(graph, "color_space", ""), "linear") || prompt || explicit_stack ||
+            (strcmp(kind, "mesh_asset_instance") && strcmp(kind, "plane") && strcmp(kind, "rect_prism"))) return false;
+        entry = find_or_create_object_material(object_materials, object_id);
+        if (!entry || cJSON_GetObjectItemCaseSensitive(entry, "material_texture_stack") ||
+            cJSON_GetObjectItemCaseSensitive(entry, "material_graph") ||
+            cJSON_GetObjectItemCaseSensitive(entry, "surface_material_binding")) return false;
+        cJSON* copy = cJSON_Duplicate(graph, 1);
+        if (!copy) return false;
+        cJSON_DeleteItemFromObjectCaseSensitive(entry, "surface_graph");
+        cJSON_AddItemToObject(entry, "surface_graph", copy);
+        return true;
+    }
