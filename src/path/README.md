# src › path

Bézier path logic used to animate the light source.

- `path_system.c` – Implements de Casteljau interpolation for quadratic and cubic segments, path mode toggling, and debug rendering helpers.
