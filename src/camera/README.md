# src › camera

Camera subsystem that tracks viewport position and zoom, and offers helpers to convert between world- and screen-space. The renderer and editors use these utilities to draw and interact with the scene consistently. Camera paths are edited alongside light paths and use shared math utilities (`math/vec2`).
