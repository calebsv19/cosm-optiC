# src › app

Application lifecycle orchestration.

- `animation.c` – Defines `main` (via `MAIN_DRIVER`), initialises SDL, loads configs, runs the animation/deep-render loop, saves frames, and optionally triggers MP4 generation.
